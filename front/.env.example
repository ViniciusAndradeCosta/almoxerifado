# Frontend — variáveis de ambiente (copie para .env e preencha)

# URL pública do backend (no Render). Em produção, defina na Vercel.
# Em desenvolvimento local, normalmente: http://localhost:3000
VITE_API_URL="https://seu-backend.onrender.com"
